<style>
	/* SECTION BACKGROUND */
	$_section {


		font-family: 'Inter', sans-serif;
		background: {{= &section_bg}};
		color: #333;
		overflow-x: hidden;


		/* Hero */

		.hero-container {

			max-width: 1440px;
			margin: auto;
			padding: 50px 30px;
			padding-right: 90px;
			display: flex;
			align-items: center;
			gap: 4%;
			position: relative;
			overflow: hidden;
		}

		/* Left Content */

		.hero-content {
			flex: 0 0 34%;

			display: flex;
			flex-direction: column;
			justify-content: center;
		}

		.sub-heading {
			font-family: 'Playfair Display', serif;
			font-size: {{= &sub_heading_size}}px;
			font-style: italic;
			color: {{= &sub_heading_color}};
			font-weight: {{= &sub_heading_weight}};
			margin-bottom: 15px;
		}

		.main-heading {
			font-family: 'Playfair Display', serif;
			font-size: {{= &main_heading_size}}px;
			line-height: 1.15;
			font-weight: {{= &main_heading_weight}};

			color: {{= &main_heading_color}};

			margin-bottom: 24px;
		}

		/* .description  */
		/* 	font-size: = &desc_size}}px; */
		/* 	line-height: 1.7; */

		/* 	color:  &desc_color}}; */
		/* 	font-weight: = &desc_weight}}; */
		/* 	margin-bottom: 40px; */
		.description {
			font-size: {{= &desc_size}}px;
			line-height: 1.7;
			color: {{= &desc_color}};
			font-weight: {{= &desc_weight}};
			margin-bottom: 40px;

			white-space: pre-line;   /* Enter key support */

			display: -webkit-box;
			-webkit-line-clamp: 2;
			-webkit-box-orient: vertical;

			overflow: hidden;
			text-overflow: ellipsis;
		}


		.shop-btn {
			width: max-content;

			display: inline-flex;
			align-items: center;
			justify-content: center;

			padding: 16px 36px;

			border-radius:{{= &btn_radius}}px;

			background: {{= &btn_bg}};
			color: {{= &btn_color}};

			text-decoration: none;
			font-weight: 600;

			margin-bottom: 60px;

			transition: 0.3s ease;
		}

		.shop-btn:hover {
			transform: translateY(-2px);
			background: {{= &btn_bg}};
		}

		/* Controls */

		.slider-controls {
			display: flex;
			align-items: center;
			gap: 16px;
			margin-bottom: 15px;
		}

		.arrow-btn {
			border: none;
			background: transparent;

			display: flex;
			align-items: center;
			justify-content: center;

			cursor: pointer;

			color: #0f2e3c;

			transition: 0.2s ease;
		}

		.arrow-btn:hover {
			opacity: 0.7;
		}

		/* Dots */

		.dots {
			display: flex;
			gap: 8px;
		}

		.dot {
			width: 6px;
			height: 6px;
			min-height: 6px;
			border-radius: 50%;
			background: #b0bac3;
			position: relative;
			overflow: hidden;
			display: flex;
			align-items: center;

		}

		.dot.active {
			width: 32px;
			border-radius: 4px;
			background: #e0e5e9;
		}

		.progress {
			position: absolute;
			top: 0;
			left: 0;
			height: 100%;
			width: 0%;
			border-radius: inherit;
			background: #0f2e3c;
		}

		.dot.active.animate .progress {
			width: 100%;
			transition: width 5s linear;
		}

		/* Image */

		.hero-image-wrapper {
			flex: 0 0 62%;
			min-width: 300px;
			flex-shrink: 0;
			height: 460px;

			overflow: hidden;
			border-radius: {{= &img_radius}}px;

			box-shadow: 0 20px 40px rgba(0, 0, 0, 0.06);
		}

		.hero-image {
			width: 100%;
			height: 100%;
			object-fit: cover;
			transition: 0.5s ease;
		}

		/* Animations */

		.fade-out {
			opacity: 0;
			transform: translateY(-10px);
		}

		.fade-in {
			animation: fadeIn 0.5s ease;
		}

		.close-banner {
			border: none;
			background: transparent;

			color: inherit;

			cursor: pointer;
		}

		/* Responsive */

		@media (max-width: 1024px) {

			.hero-container {
				padding: 40px 20px;
				padding-right: 80px;
			}

			.hero-content {
				flex: 0 0 45%;
			}

			.hero-image-wrapper {
				flex: 0 0 50%;
				height: 480px;
			}
		}

		@media (max-width: 768px) {

			.shop-btn {
				font-size:{{= &btn_size}}px;
				margin-bottom: 35px;
			}

			/* .slider-controls  */
			/* 	justify-content: center; */
			/* 	margin-top: 0; */
			/* 	margin-bottom: 0; */
			/* 	transform: translateY(-10px); */
			/* } */

			.hero-container {
				flex-direction: column-reverse;
				padding: 40px 20px;
			}

			.hero-content {
				width: 100%;
				text-align: center;
				align-items: center;
			}

			.hero-image-wrapper {
				width: {{= &img_size}}px;
				height: auto;

				max-width: 100%;
				margin: auto;
			}

			.hero-image {
				width: 100%;
				height: auto;

				object-fit: contain;
				display: block;
			}


			.sub-heading{
				font-size:{{= &m_sub_size}}px;
				margin-top:21px ;
			}

			.main-heading {
				font-size: {{= &m_m_heading_size}}px;
			}

			.description {
				font-size: {{= &desc_size}}px;
			}
		}



	}
	@keyframes fadeIn {

		from {
			opacity: 0;
			transform: translateY(20px);
		}

		to {
			opacity: 1;
			transform: translateY(0);
		}
	}
	/* custom css */

	$_section{
		{{= &custom_css}}

	}
</style>

<div class="{{= &section_spacing}} ">
	<div class="hero-container pt-md-{{= &p_md_top}} pb-md-{{= &p_md_bottom}} px-md-{{= &p_l_r_md}}">

		<!-- Left Content -->
		<div class="hero-content">

			<span class="$_s_class-sub-heading sub-heading"></span>

			<h1 class="main-heading" id="$_s_class-hero-title"></h1>


			<p class="$_s_class-description description"></p>


			<a href="#" class="$_s_class-shop-btn shop-btn"></a>

			<!-- Slider Controls -->

			<div class="$_s_class-slider-controls slider-controls">
				<button class="arrow-btn" id="$_s_class-prev-btn" aria-label="Previous Slide">

					<svg
					width="20"
					height="20"
					viewBox="0 0 24 24"
					fill="none"
					stroke="currentColor"
					stroke-width="2"
					stroke-linecap="round"
					stroke-linejoin="round"
					>
					<path d="m15 18-6-6 6-6" />
				</svg>

				</button>
			

				<div class="$_s_class-dots dots"></div>
				<button class="arrow-btn" id="$_s_class-prev-btn" aria-label="Previous Slide">
					<button class="arrow-btn" id="$_s_class-next-btn" aria-label="Next Slide">

						<svg
						width="20"
						height="20"
						viewBox="0 0 24 24"
						fill="none"
						stroke="currentColor"
						stroke-width="2"
						stroke-linecap="round"
						stroke-linejoin="round"
						>
						<path d="m9 18 6-6-6-6" />
					</svg>

				</button>

			</div>

		</div>

		<!-- Right Image -->
		<div class="hero-image-wrapper">

			<img
			src=""
			alt="Beauty Product"
			class="hero-image"
			id="$_s_class-hero-image"
			/>

		</div>
	</div>
</div>

<script>
	document.addEventListener("DOMContentLoaded", () => {
		const $_s_class_slides = [


		];

		{{with each &slide_component as $slide}}
			$_s_class_slides.push({{use component $slide}})
		{{endwith}}

		console.log("slides: "+$_s_class_slides)


		/* Create Dots Dynamically */

		const dotsContainer = document.querySelector("$_section-dots");

		$_s_class_slides.forEach((_, index) => {

			const dot = document.createElement("div");

			dot.classList.add("dot");

			if(index === 0){
				dot.classList.add("active");
			}

			dot.setAttribute("data-index", index);

			dot.innerHTML = `<div class="progress"></div>`;

			dotsContainer.appendChild(dot);
		});

		/* Select Dots */

		const dots = document.querySelectorAll("$_section .dot");

		/* Hide Controls if Only One Slide */



		const sliderControls = document.querySelector("$_section-slider-controls");

		if ($_s_class_slides.length <= 1) {
			sliderControls.style.display = "none";
		}

		let currentSlide = 0;
		let interval;

		/* Elements */

		const subHeading = document.querySelector("$_section-sub-heading");
		const title = document.getElementById("$_s_class-hero-title");
		const description = document.querySelector("$_section-description");
		const button = document.querySelector("$_section-shop-btn");
		const image = document.getElementById("$_s_class-hero-image");



		const prevBtn = document.getElementById("$_s_class-prev-btn");
		const nextBtn = document.getElementById("$_s_class-next-btn");

		/* Render Slide */

		function renderSlide() {

			const slide = $_s_class_slides[currentSlide];

			subHeading.innerHTML = slide.subHeading;
			title.innerHTML = slide.title;
			description.textContent = slide.description;

			button.innerHTML = slide.btnText;
			button.href = slide.btnLink;

			image.src = slide.image;

			animateContent();

			updateDots();
		}

		/* Animation */

		function animateContent() {

			const elements = [
			subHeading,
			title,
			description,
			button,
			image
			];

			elements.forEach(el => {

				el.classList.remove("fade-in");

				void el.offsetWidth;

				el.classList.add("fade-in");
			});
		}

		/* Dots */

		function updateDots() {

			dots.forEach((dot, index) => {

				dot.classList.remove("active", "animate");

				if (index === currentSlide) {

					dot.classList.add("active");

					setTimeout(() => {
						dot.classList.add("animate");
					}, 10);
				}
			});
		}

		/* Next */

		function nextSlide() {

			currentSlide =
			(currentSlide + 1) % $_s_class_slides.length;

			renderSlide();

			resetInterval();
		}

		/* Previous */

		function prevSlide() {

			currentSlide =
			(currentSlide - 1 + $_s_class_slides.length) %
			$_s_class_slides.length;

			renderSlide();

			resetInterval();
		}

		/* Auto Slide */

		function startSlider() {

			interval = setInterval(nextSlide, 5000);
		}

		function resetInterval() {

			clearInterval(interval);

			startSlider();
		}

		/* Events */

		nextBtn.addEventListener("click", nextSlide);

		prevBtn.addEventListener("click", prevSlide);

		dots.forEach((dot, index) => {

			dot.addEventListener("click", () => {

				currentSlide = index;

				renderSlide();

				resetInterval();
			});
		});

		/* Init */

		renderSlide();

		startSlider();
	});
</script>