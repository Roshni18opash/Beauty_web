<style>
font-family:'Inter', sans-serif;
  background: #fff;
  color: #0f2e3c;

$_section{
	
.container {
  max-width: 1440px;
  margin: 0 auto;
  padding: 50px 24px 36px;
}

/* Header */
.section-header {
  display: flex;
  justify-content: space-between;
  align-items: flex-end;
  margin-bottom: 28px;
}

.section-title {
  font-family: 'Playfair Display', Georgia, serif;
  font-size: 2.5rem;
  font-weight: 500;
  margin-bottom: 8px;
}

.section-desc {
  font-size: 0.875rem;
  color:#707070;
}

.tabs {
  display: flex;
  gap: 22px;
}

.tab {
  border: none;
  background: none;
  font-size: 0.9rem;
  font-weight: 600;
  color:#707070;
  cursor: pointer;
  padding-bottom: 6px;
  position: relative;
}

.tab.active {
  color: #0f2e3c;
}

.tab.active::after {
  content: '';
  position: absolute;
  left: 0;
  bottom: 0;
  width: 100%;
  height: 2px;
  background: #000;
}

/* Row — ~5 full cards + peek of 6th (like reference) */
.products-track {
  display: flex;
  align-items: stretch;
  gap: var(--gap);
  overflow-x: auto;
  scroll-behavior: smooth;
  scrollbar-width: none;
  padding-bottom: 4px;
}
.products-track::-webkit-scrollbar {
  display: none;
}

.product-card {
  flex: 0 0 220px;
  width:;
  background:#f6f6f6;
  border-radius: 16px;
  overflow: hidden;
  display: flex;
  flex-direction: column;
}

/* Desktop: show 5 full cards + half of 6th */
@media (min-width: 769px) {
  .product-card {
    flex-basis: calc((100% - (var(--gap) * 5)) / 5.5);
    width: calc((100% - (var(--gap) * 5)) / 5.5);
  }
}
.card-image {
  position: relative;
  height: auto;
  min-height: 260px;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 36px 16px 28px;
  overflow: hidden;
}

.img-box {
  position: static;
  display: contents;
}

/* Default product image — centered, contained */
.img-main {
  position: relative;
  z-index: 1;
  width: auto;
  height: auto;
  max-width: 90%;
  max-height: 200px;
  object-fit: contain;
  transition: opacity 0.35s ease;
}

/* Hover image — stretches to fill the whole card-image area */
.img-alt {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  object-fit: cover;
  opacity: 0;
  z-index: 0;
  transition: opacity 0.35s ease;
}

.card-image:hover .img-main {
  opacity: 0;
}

.card-image:hover .img-alt {
  opacity: 1;
}



.badge-new {
  position: absolute;
  top: 12px;
  left: 12px;
  background: #82b1a5;
  color: #fff;
  font-size: 0.65rem;
  font-weight: 600;
  padding: 5px 11px;
  border-radius: 50px;
  z-index: 2;
}

.selling-fast {
  position: absolute;
  left: 0;
  right: 0;
  bottom: 0;
  background: #fff;
  overflow: hidden;
  padding: 6px 0;
  z-index: 1;
}

.selling-fast-track {
  display: flex;
  width: max-content;
  animation: marquee 15s linear infinite;
}

.selling-fast-track span {
  display: inline-flex;
  align-items: center;
  gap: 10px;
  padding-right: 10px;
  font-size: 0.65rem;
  font-weight: 600;
  white-space: nowrap;
}

.selling-fast-track i {
  font-style: normal;
  color: #f5a623;
}

@keyframes marquee {
  to { transform: translateX(-50%); }
}

/* Image hover */
.btn-search,
.btn-cart {
  position: absolute;
  border: none;
  cursor: pointer;
  opacity: 0;
  transition: opacity 0.3s, transform 0.3s;
  z-index: 3;
}

.btn-search {
  top: 12px;
  right: 12px;
  width: 36px;
  height: 36px;
  border-radius: 50%;
  background: #fff;
  display: flex;
  align-items: center;
  justify-content: center;
  transform: translate(8px, -8px);
}

.btn-cart {
  left: 12px;
  right: 12px;
  bottom: 12px;
  padding: 11px;
  border-radius: 50px;
  background: #fff;
  font-size: 0.8rem;
  font-weight: 600;
  overflow: hidden;
  transform: translateY(16px);
}

.card-image:has(.selling-fast) .btn-cart {
  bottom: 34px;
}

.btn-cart::before {
  content: '';
  position: absolute;
  inset: 0;
  background:#0f2e3c;
  transform: scaleX(0);
  transform-origin: left;
  transition: transform 0.3s;
  z-index: 0;
}

.btn-cart span {
  position: relative;
  z-index: 1;
}

.btn-cart:hover::before {
  transform: scaleX(1);
}

.btn-cart:hover {
  color: #fff;
}

.card-image:hover .btn-search,
.card-image:hover .btn-cart {
  opacity: 1;
}

.card-image:hover .btn-search {
  transform: translate(0, 0);
}

.card-image:hover .btn-cart {
  transform: translateY(0);
}

.card-info {
  padding: 14px 16px 18px;
  display: flex;
  flex-direction: column;
  flex: 1;
}

.price {
  font-size: 1rem;
  font-weight: 500;
  margin-top: auto;
}
.category {
  display: block;
  font-size: 0.7rem;
  font-weight: 600;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  color:#707070;
  margin-bottom: 6px;
}
.name {
  display: -webkit-box;
  font-family: 'Inter', sans-serif;
  font-size: 15px;
  font-weight: 600;
  margin-bottom: 4px;
  line-height: 1.3;

  -webkit-box-orient: vertical;
  -webkit-line-clamp: 2;
  line-clamp: 2;

  overflow: hidden;
}


.hover-line {
  position: relative;
  display: inline-block;
  max-width: 100%;
}

.hover-line::after {
  content: '';
  position: absolute;
  left: 0;
  bottom: -2px;
  width: 100%;
  height: 1px;
  background: currentColor;
  transform: scaleX(0);
  transform-origin: right;
  transition: transform 0.3s;
}

.hover-line:hover::after {
  transform: scaleX(1);
  transform-origin: left;
}

/* Footer */
.slider-footer {
  display: flex;
  align-items: center;
  gap: 20px;
  margin-top: 28px;
}

.scroll-bar-wrap {
  flex: 1;
  position: relative;
  height: 4px;
}

.scroll-track {
  width: 100%;
  height: 1px;
  background: #e4e4e4;
}

.scroll-thumb {
  position: absolute;
  left: 0;
  top: 50%;
  transform: translateY(-50%);
  height: 3px;
  width: 30%;
  background: #1a1a1a;
  transition: left 0.1s;
}

.slider-nav {
  display: flex;
  gap: 8px;
}

.nav-btn {
  width: 40px;
  height: 40px;
  border-radius: 50%;
  border: 1px solid #e5e5e5;
  background: #fff;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
}

/* Mobile */
@media (max-width: 768px) {
  :root {
    --card-w: 200px;
  }

  .section-header {
    flex-direction: column;
    align-items: flex-start;
    gap: 16px;
  }

  .section-title {
    font-size: 2rem;
  }
}

@media (max-width: 480px) {
  :root {
    --card-w: 75%;
  }
}
	}
	$_section{
		{{= &custom_css}}
	}

</style>
{{$collection_prods = system.collection.get_collection_products(&collections)}}
<section class="new-arrivals">
    <div class="container">

      <header class="section-header">
        <div>
          <h2 class="section-title">New Arrivals</h2>
          <p class="section-desc">Traditional divides between personal and professional space.</p>
        </div>
        <div class="tabs">
          <button class="tab active" type="button">New Arrivals</button>
          <button class="tab" type="button">Best Sellers</button>
        </div>
      </header>

      <div class="slider-wrap">
        <div class="products-track" id="productsTrack">

    		      <!-- <article class="product-card"> -->
          <!--   <div class="card-image"> -->
          <!--     <span class="badge-new">New</span> -->
          <!--     <div class="img-box"> -->
          <!--       <img class="img-main" src="images/new1.webp" alt="Turn Chair Soft"> -->
          <!--       <img class="img-alt" src="images/new-1.webp" alt=""> -->
          <!--     </div> -->
          <!--     <div class="selling-fast"> -->
          <!--       <div class="selling-fast-track"> -->
          <!--         <span>Selling Fast <i>⚡</i> Selling Fast <i>⚡</i> Selling Fast <i>⚡</i> </span> -->
          <!--         <span>Selling Fast <i>⚡</i> Selling Fast <i>⚡</i> Selling Fast <i>⚡</i> </span> -->
          <!--       </div> -->
          <!--     </div> -->
          <!--     <button class="btn-search" type="button" aria-label="Search"> -->
          <!--       <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="11" cy="11" r="8"/><path d="m21 21-4.3-4.3"/></svg> -->
          <!--     </button> -->
          <!--     <button class="btn-cart" type="button">Add To Cart</button> -->
          <!--   </div> -->
          <!--   <div class="card-info"> -->
          <!--     <span class="category hover-line">Serum</span> -->
          <!--     <h3 class="name hover-line">Turn Chair Soft</h3> -->
          <!--     <p class="price">$40.00</p> -->
          <!--   </div> -->
      			    <!-- </article> -->
       {{with each $collection_prods as $product}}
			{{$prods = system.product.get_product($product)}}
          <article class="product-card">
            <div class="card-image">
              <span class="badge-new">New</span>
              <div class="img-box">
                <img class="img-main" src="images/new2.webp" alt="Balancing Hypotonic">
                <img class="img-alt" src="images/new-2.webp" alt="">
              </div>
              <button class="btn-search" type="button" aria-label="Search">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="11" cy="11" r="8"/><path d="m21 21-4.3-4.3"/></svg>
              </button>
              <button class="btn-cart" type="button">Add To Cart</button>
            </div>
            <div class="card-info">
              <span class="category hover-line">Shampoo</span>
              <h3 class="name hover-line">Balancing Hypotonic</h3>
              <p class="price">$35.00</p>
            </div>
          </article>

        </div>
      </div>

      <div class="slider-footer">
        <div class="scroll-bar-wrap">
          <div class="scroll-track"></div>
          <div class="scroll-thumb" id="scrollThumb"></div>
        </div>
        <div class="slider-nav">
          <button class="nav-btn" id="prevBtn" type="button" aria-label="Previous">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="m15 18-6-6 6-6"/></svg>
          </button>
          <button class="nav-btn" id="nextBtn" type="button" aria-label="Next">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="m9 18 6-6-6-6"/></svg>
          </button>
        </div>
      </div>

    </div>
  </section>

  <script>
	const track = document.getElementById('productsTrack');
const thumb = document.getElementById('scrollThumb');

document.querySelectorAll('.btn-cart').forEach((btn) => {
  btn.innerHTML = '<span>' + btn.textContent.trim() + '</span>';
});

document.getElementById('prevBtn').onclick = () => {
  const w = track.querySelector('.product-card').offsetWidth + 16;
  track.scrollBy({ left: -w, behavior: 'smooth' });
};

document.getElementById('nextBtn').onclick = () => {
  const w = track.querySelector('.product-card').offsetWidth + 16;
  track.scrollBy({ left: w, behavior: 'smooth' });
};

function updateBar() {
  const max = track.scrollWidth - track.clientWidth;
  if (max <= 0) return;
  const w = (track.clientWidth / track.scrollWidth) * 100;
  thumb.style.width = w + '%';
  thumb.style.left = (track.scrollLeft / max) * (100 - w) + '%';
}

track.onscroll = updateBar;
window.onresize = updateBar;
updateBar();

document.querySelectorAll('.tab').forEach((tab) => {
  tab.onclick = () => {
    document.querySelectorAll('.tab').forEach((t) => t.classList.remove('active'));
    tab.classList.add('active');
  };
});

  </script>