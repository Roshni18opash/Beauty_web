<style>
	/* bg color of section */
	$_section{
		background-color: {{= &section_bg_color}};
	}

	/* slider top bottom padding */
	.swiper{
		padding: 10px 0 !important;
	}

	/* slider container */
	.category-slider-container{
		overflow: hidden;
		position: relative; /* arrow work in absolute position */
	}

	/* img hover animation */
	.category-img-wrapper{
		overflow: hidden;
		transition: transform .3s ease;
	}

	.swiper-slide:first-child .category-img-wrapper{
		background: transparent;
	}

	.category-img{
		width: 100%;
		height: 100%;
		object-fit: cover;
		border-radius: {{= &image_radious}}%;
	}

	.category-text{
		{{if &title != 'y'}}
			display: none;
		{{endif}}

		font-family: 'Figtree', sans-serif;
		font-weight: {{= &text_weight}};
		color: {{= &text_color}} !important;
		line-height: 1.2;
		transition: .3s ease;
	}

	/* slider dot */
	.swiper-pagination-bullet{
		background-color: {{= &m_indicator_background_color}} !important;
	}

	/* HOVER EFFECTS */

	/* img zoom on hover */
	.category-item:hover .category-img-wrapper{
		transform: scale(1.05);
	}

	/* text title left move */
	.category-item:hover .category-text{
		transform: translateX(-4px);
	}

	/* NAVIGATION BUTTONS */

	.custom-nav-btn{
		border-radius: 50%;
		color: {{= &arrow_color}} !important;
		background-color: rgb(from {{= &arrow_bg_color}} r g b / {{= &arrow_bg_opacity}}%) !important;
		box-shadow: 0 2px 5px rgba(0,0,0,.2);
		overflow: hidden;
		z-index: 10;
		/* transition: .3s ease; */
		transition: all .4s ease;
		position: absolute;
		top: 40%;
		opacity: 0;
	}

	.custom-nav-btn.swiper-button-disabled{
		opacity: 0 !important;
		pointer-events: none;
		
	}

	/* arrow icon */
	.custom-nav-btn::after{
		font-weight: bold;
		transition: .3s ease;
		position: relative;
		z-index: 3;
		font-size: 14px !important;
		color: {{= &arrow_color}} !important;
	}

	/* sliding background */
	.custom-nav-btn::before{
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		background-color: {{= &arrow_bg_color}} !important;
		transform: translateX(-100%);
		transition: transform .4s ease;
		z-index: 1;
	}

	/* hover animation */
	.custom-nav-btn:hover::before{
		transform: translateX(0);
		color: {{= &arrow_color}} !important;
	}

	/* hover arrow color */
	.custom-nav-btn:hover::after{
		color: {{= &arrow_color}} !important;
	}

	/* show arrows on section hover */
	.category-slider-container:hover .swiper-button-prev.custom-nav-btn{
		opacity: 1;
	}

	.category-slider-container:hover .swiper-button-next.custom-nav-btn{
		opacity: 1;
	}

	/* =========================
	   MOBILE CSS
	========================= */

	@media (max-width: 767px){

		.category-slider-container{
			padding: 10px;
		}

		.category-text{
			font-size: calc({{= &text_size}}px * .85);
			margin-top: 8px;
		}

		.custom-nav-btn{
			width: 32px;
			height: 32px;
			margin-top: -16px;
			color: {{= &m_arrow_color}} !important;
		}

		/* mobile hover bg */
		.custom-nav-btn::before{
			background-color: {{= &m_arrow_bg_color}} !important;
		}
		.custom-nav-btn:hover::after{
			color: {{= &m_arrow_color}} !important;}

		.custom-nav-btn::after{
			font-size: 12px !important;
		}

		.swiper-button-prev.custom-nav-btn{
			left: -15px;
		}

		.swiper-button-next.custom-nav-btn{
			right: -15px;
		}

		.category-slider-container:hover .swiper-button-prev.custom-nav-btn{
			left: 5px;
		}

		.category-slider-container:hover .swiper-button-next.custom-nav-btn{
			right: 5px;
		}
	}

	$_section{
		{{= &custom_css}}
	}

	/* =========================
	   TABLET CSS
	========================= */

	@media (min-width: 768px) and (max-width: 1023px){

		.category-slider-container{

			{{if &p_l_r_md == 'custom'}}
				padding-left: {{= &custom_padding}}px !important;
				padding-right: {{= &custom_padding}}px !important;
			{{endif}}
		}

		.category-text{
			font-size: {{= &text_size}}px;
			margin-top: 10px;
		}

		.custom-nav-btn{
			width: 40px;
			height: 40px;
			margin-top: -20px;
		}

		.custom-nav-btn::after{
			font-size: 14px !important;
		}

		.swiper-button-prev.custom-nav-btn{
			left: -15px;
		}

		.swiper-button-next.custom-nav-btn{
			right: -15px;
		}

		.category-slider-container:hover .swiper-button-prev.custom-nav-btn{
			left: 10px;
		}

		.category-slider-container:hover .swiper-button-next.custom-nav-btn{
			right: 10px;
		}
	}

	/* =========================
	   DESKTOP CSS
	========================= */

	@media (min-width: 1024px){

		.category-img:hover{
			opacity: .95;
		}

		.category-text{
			font-size: {{= &text_size}}px;
			margin-top: 10px;
		}

		.custom-nav-btn{
			width: 42px;
			height: 42px;
			margin-top: -21px;
		}

		.custom-nav-btn::after{
			font-size: 15px !important;
		}

		.swiper-button-prev.custom-nav-btn{
			left: -15px;
		}

		.swiper-button-next.custom-nav-btn{
			right: -15px;
		}

		.category-slider-container:hover .swiper-button-prev.custom-nav-btn{
			left: 10px;
		}

		.category-slider-container:hover .swiper-button-next.custom-nav-btn{
			right: 10px;
		}
	}
</style>

<div class="container">
	<div class="category-slider-container pt-md-{{= &p_md_top}} pb-md-{{= &p_md_bottom}} px-md-{{= &p_l_r_md}} position-relative">

		<!-- Swiper -->
		<div class="swiper mySwiper $_s_class-mySwiper category-swiper">
			<div class="swiper-wrapper">
				{{with each &collection as $slideimage at $index}}
					{{use component $slideimage}}
				{{endwith}}
			</div>
		</div>

		<!-- Pagination -->
		{{if &indicator_options == "indicator" || &indicator_options == "both" || &mobile_indicator_options == "indicator" || &mobile_indicator_options == "both"}}
			<div class="swiper-pagination"></div>
		{{endif}}

		<!-- Navigation -->
		{{if &indicator_options == "arrow" || &indicator_options == "both" || &mobile_indicator_options == "arrow" || &mobile_indicator_options == "both"}}
			<div class="swiper-button-prev custom-nav-btn"></div>
			<div class="swiper-button-next custom-nav-btn"></div>
		{{endif}}

	</div>
</div>

<script>
	var swiper = new Swiper(".$_s_class-mySwiper", {

		slidesPerView: {{= &slidepreview}},
		spaceBetween: 10,
		slidesPerGroup: 1,

		loop: {{= &slide_looping == 'y' ? "true" : "false"}},

		autoplay: {{= &auto_play == 'y'
				? "{ delay: " + (&auto_play_speed * 1000) + ", disableOnInteraction: false }"
				: "false"
		}},

		breakpoints: {

			0: {
				slidesPerView: {{= &item_to_show_mobile}},
				spaceBetween: 10,

				pagination: {
					el: ".swiper-pagination",
					clickable: true,
					enabled: {{= &mobile_indicator_options == "indicator" || &mobile_indicator_options == "both" ? "true" : "false"}}
				},

				navigation: {
					nextEl: ".swiper-button-next",
					prevEl: ".swiper-button-prev",
					enabled: {{= &mobile_indicator_options == "arrow" || &mobile_indicator_options == "both" ? "true" : "false"}}
				}
			},

			768: {
				slidesPerView: {{= &slidepreview}},
				spaceBetween: {{= &spacebetween}},

				pagination: {
					el: ".swiper-pagination",
					enabled: {{= &indicator_options == "indicator" || &indicator_options == "both" ? "true" : "false"}}
				},

				navigation: {
					nextEl: ".swiper-button-next",
					prevEl: ".swiper-button-prev",
					enabled: {{= &indicator_options == "arrow" || &indicator_options == "both" ? "true" : "false"}}
				},

				mousewheel: true,
				keyboard: true
			}
		}
	});
</script>